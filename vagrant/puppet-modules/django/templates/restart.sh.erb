#!/bin/sh
# !! THIS FILE IS BASED ON A TEMPLATE MANAGED BY PUPPET !!
# Dans mod_wsgi en Daemon mode, il suffit de changer le script principal pour causer un restart
# C'est ce qu'on fait ici

touch <%=projectroot%>/<%=sitename%>.wsgi
